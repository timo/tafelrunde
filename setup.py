VERSION = '0.0.1'

from setuptools import setup, find_packages

setup(
      name = 'tableround',
      version = VERSION,
      author = 'zasim team',
      description = 'a git-aware benchmarking system for python projects',
      keywords = '',
      url = '',
      packages = find_packages(),
    )
